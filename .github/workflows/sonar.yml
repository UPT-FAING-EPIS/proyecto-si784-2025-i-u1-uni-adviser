name: SonarCloud Analysis for PHP

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  sonarcloud:
    name: SonarCloud Scan
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'  # Puedes cambiar a la versión que uses
          extensions: mbstring, xml, ctype, iconv, intl, zip
          tools: composer

      - name: Install dependencies
        run: composer install --no-progress --no-suggest

      - name: Install SonarScanner
        run: |
          npm install -g sonarqube-scanner

      - name: Run SonarCloud analysis
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          sonar-scanner \
            -Dsonar.projectKey=apibankk_proyecto \
            -Dsonar.organization=apibankk \
            -Dsonar.sources=. \
            -Dsonar.host.url=https://sonarcloud.io \
            -Dsonar.login=${{ secrets.SONAR_TOKEN }} \
            -Dsonar.exclusions=vendor/**,**/*.test.*,**/tests/**,node_modules/**
